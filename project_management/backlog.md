# Backlog

All tasks in the previous backlog have been completed:

1. ✓ Added more dict types into types.py including proper documentation and type hints
2. ✓ Modified csv_parser.py to skip duplicate entries using normalized URL comparison
3. ✓ Instance ID in temporary file names was already implemented in video_renderer.py

No pending tasks in the backlog.